# 如何在 Chrome 中安裝自動應援擴充功能

1. 點擊本頁面右上角的綠色按鈕「Code」。
2. 選擇「Download ZIP」。
3. 解壓縮下載的檔案。
4. 打開 Chrome 瀏覽器，並在網址列輸入：`chrome://extensions/`。
5. 在右上角開啟「開發人員模式」。
6. 點擊「載入未封裝項目」。
7. 進入解壓縮的檔案夾 選擇auto_supporter 資料夾。


